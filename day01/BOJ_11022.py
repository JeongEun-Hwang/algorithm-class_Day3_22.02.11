T = int(input())

for i in range(T):
    a,b = map(int,input().split())
    print(f'Case #{i+1} : {a}+{b}={a+b}')  


# 리스트
# immutable한 객체 : 변경이 불가능하다. ==> 수정한다고 원래 객체가 변화하는것이 아닌 새로운 객체가 생성되는 것이다. 
# 슬라이싱
# 슬라이싱 한다고 원래 변수의 변수 값이 변경되지 않는다. 

